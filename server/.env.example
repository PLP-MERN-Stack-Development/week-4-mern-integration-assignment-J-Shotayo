MONGODB_URI=mongodb://localhost:27017/mern-blog
PORT=5000
JWT_SECRET=your_token_secret_key
# Note: Replace `your_token_secret_key` with a secure key for JWT signing.
# Ensure that the MongoDB URI points to your local MongoDB instance.
# If you deploy your application, update the CORS_ORIGIN to match your production URL.